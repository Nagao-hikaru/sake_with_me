 <% if user_signed_in? %>
        <% if current_user.already_liked?(@restaurant) %>
          <%= button_to 'いいねを取り消す', restaurant_like_path(@restaurant), method: :delete %>
        <% else %>
          <%= button_to 'いいね', restaurant_likes_path(@restaurant) %>
        <% end %>
       <%= @restaurant.likes.count %>
      <% end %>